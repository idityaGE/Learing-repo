const _greet = (name: string): string => {
  return `Hello ${name}!`;
};

// console.log(greet("deno")); // Hello deno!

// fmt -> "deno fmt messy.ts" or "deno fmt"
// help to format the code


// lint -> "deno lint messy.ts" or "deno lint"