<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 100px;
            padding: 0;
            background-color: #f4f4f4;
        }

        h1 {
            margin: 20px;
            padding: 10px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: inline-block;
        }

        form {
            margin: 20px;
            padding: 10px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: inline-block;
        }

        input {
            padding: 5px 2px;
            width: 100%;
        }

        textarea {
            margin: 10px 0;
            padding: 5px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 100%;
        }

        button {
            margin-left: 25px;
            margin-top: 10px;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        div {
            display: flex;
            flex-direction: column;
        }
    </style>
</head>

<body>
    <h1>Edit Chat</h1>
    <div>
        <h2>From : <%= chat.from %>
            <h3>Id: <%= chat._id %></h3>
        </h2>
        <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
            <textarea name="msg" id="" cols="20" rows="10"><%= chat.msg %></textarea>
            <button type="submit">Save</button>
        </form>
        <h2>To : <%= chat.to %></h2>
    </div>
</body>

</html>